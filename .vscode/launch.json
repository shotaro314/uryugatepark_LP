{
    "version": "0.2.0",
    "configurations": [
        
        { //chrome debagger設定
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "file": "${file}"
        }
    ]
 }